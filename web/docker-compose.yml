services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - sqlite-data:/app/database
    env_file:
      - ./backend/${ENV_FILE:-.env.local}
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:///./database/aquarius.db}
      - TURSO_AUTH_TOKEN=${TURSO_AUTH_TOKEN:-}
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - aquarius-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - aquarius-network
    command: sh -c "chmod +x /app/node_modules/.bin/* && npm run dev"

  # Playwright service - disabled due to ARM architecture issues (ADR-029)
  # E2E tests run locally via 'make test-ui' instead
  # playwright:
  #   image: mcr.microsoft.com/playwright:v1.57.0-jammy
  #   working_dir: /app
  #   environment:
  #     - PLAYWRIGHT_BASE_URL=http://frontend:5173
  #   volumes:
  #     - ./frontend:/app
  #   depends_on:
  #     frontend:
  #       condition: service_started
  #     backend:
  #       condition: service_healthy
  #   networks:
  #     - aquarius-network
  #   command: /bin/sh -c "npm ci && npm run test:e2e"

volumes:
  sqlite-data:

networks:
  aquarius-network:
    driver: bridge
